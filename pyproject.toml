[project]
name = "sentinel-archive"
version = "0.1.0"
description = "Standalone market replay and Edge/Pulse contract Sentinel Archive."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
